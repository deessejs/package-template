# CI publish auth is handled by actions/setup-node (registry-url + NODE_AUTH_TOKEN),
# so no token reference belongs here — it would warn on every local install.
auto-install-peers=true
strict-peer-dependencies=false
