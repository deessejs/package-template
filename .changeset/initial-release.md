---
'@deessejs/example': minor
---

First tracked release of `@deessejs/example` from the package-template. Scopes the
package, adds a `files` allowlist, switches to `NodeNext` resolution, and moves tests
into `packages/example/tests/`.
