---
'@deessejs/example': minor
---

Add `pnpm setup` for one-shot package rename, `pkg.pr.new` preview
releases on every PR and push to `dev`, and switch npm publishing to
Trusted Publishing (OIDC). No more `NPM_TOKEN`.
