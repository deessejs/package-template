# Changesets

This directory holds pending version bumps. Files here are consumed and deleted when a
release is cut.

Run `pnpm changeset` to record a change, then commit the generated markdown file
alongside your code. See the [Changesets docs](https://github.com/changesets/changesets)
for details.
